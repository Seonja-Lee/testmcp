# plan.md

이 프로젝트의 목표와 범위. 최초 계획은 `.claude/plans/lovely-humming-wombat.md`에서
사용자 승인을 받았고, 그 내용을 프로젝트 안으로 옮겨 기록한다.

## 목표

사용자가 지정한 폴더 안의 비정형 문서(PDF, PPTX, DOCX, SVG, PNG)를 읽어서, 이 MCP
서버를 호출하는 호스트(Claude Code/Codex)가 **내용 요약**과 **폴더 구조 분석**을
할 수 있도록 원문 텍스트/이미지를 그대로 돌려준다.

## 범위

- `list_folder_structure(path, max_depth)`: 폴더 트리, 확장자별 파일 개수, 총 용량.
- `read_document(path, max_chars, max_pages)`: 확장자별 텍스트/이미지 추출.
- 지원 포맷: `.pdf .pptx .docx .svg .png .jpg .jpeg .bmp .gif .webp`.
- 요약 로직 자체는 이 서버의 범위 밖 — 호출자가 수행.

## 향후 확장 (아직 착수하지 않음)

- 허용 폴더 allowlist(환경변수)를 추가해 접근 가능한 경로 범위를 제한한다.
  현재는 호출마다 임의 경로를 받을 수 있어, 신뢰하는 로컬 환경에서 개인용으로
  쓰는 것을 전제로 한다.
- PDF 페이지를 이미지로도 렌더링(`pypdfium2` 등)해 표·차트 같은 시각 정보까지
  전달한다. 지금은 텍스트만 추출한다.
- 대용량 폴더를 반복 조회할 때를 위한 캐싱/증분 스캔.

## 비목표 (의도적으로 하지 않는 것)

- 서버 내부에서 LLM을 호출해 요약문을 생성하는 것 (설계 원칙 위반 — decisions.md 참고).
- OCR 기반 이미지 텍스트 추출 (이미지는 그대로 반환해 호출자의 비전 능력을 사용).

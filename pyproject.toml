[project]
name = "file-analysis-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Seonja-Lee" }
]
requires-python = ">=3.12"
dependencies = [
    "mcp>=2.1.1",
    "pillow>=12.3.0",
    "pypdf>=6.16.2",
    "python-docx>=1.2.0",
    "python-pptx>=1.0.2",
    "reportlab>=5.0.1",
    "svglib>=2.2.0",
]

[project.scripts]
file-analysis-mcp = "file_analysis_mcp:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
